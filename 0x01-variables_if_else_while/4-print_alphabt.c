#include <stdio.h>
int main(void)
	char lowrcase;
	printf("lowercase alphabet: ");
	for (lowercase = "a"; lowercase <= "z"; lowercase 
			{
			printf("%c ", lowercaser);
			}
			printf("\n");
			return 0;

