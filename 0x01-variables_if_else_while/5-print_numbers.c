#include <stdio.h>

int main() {
    int number;

    printf("Single-digit numbers: ");
    for (number = 0; number <= 9; number++) {
        printf("%d ", number);
    }

    printf("\n");

    return 0;
}
